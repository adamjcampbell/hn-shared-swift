android.useAndroidX=true
org.gradle.jvmargs=-Xmx2g
kotlin.code.style=official

# Skip's exported AARs all declare namespace `hacker.news.reader`, so we can't
# enforce unique package names. AGP 9 flipped the default from false to true.
android.uniquePackageNames=false
